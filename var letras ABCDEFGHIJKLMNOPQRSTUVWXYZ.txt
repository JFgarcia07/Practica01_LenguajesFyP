var letras = "ABCDEFGHIJKLMNOPQRSTUVWXYZ".split("");
var digitos = "0123456789".split("");
var signosPuntuacion = [".", ",", ";", ":"];
var operadoresAritmeticos = ["^", "*", "/", "+", "-"];
var operadoresRacionales = ["<", ">", "<=", ">="];
var operadoresLogicos = ["AND", "&&", "OR", "||"];
var signosDeAgrupacion = ["(", ")", "[", "]", "{", "}"];
var asignacion = ["="];


let Tletras = [], Tdigitos = [], TsignosPuntuacion = [], ToperadoresAritmeticos = [],
        ToperadoresRacionales = [], ToperadoresLogicos = [], TsignosAgrupacion = [], Tasignacion = [], errores = [];
    let palabras = [];
    palabras = entrada.split(" ");

    for (let palabra of palabras) {
        if (letras.includes(palabra.toUpperCase())) {
            Tletras.push(palabra);
        } else if (digitos.includes(palabra)) {
            if (digitos > 10 || digitos < 0){
                errores.push(palabra);
            } else {
                Tdigitos.push(palabra);
            }
        } else if (signosPuntuacion.includes(palabra)) {
            TsignosPuntuacion.push(palabra);
        } else if (operadoresAritmeticos.includes(palabra)) {
            ToperadoresAritmeticos.push(palabra);
        } else if (operadoresRacionales.includes(palabra)) {
            ToperadoresRacionales.push(palabra);
        } else if (operadoresLogicos.includes(palabra)) {
            ToperadoresLogicos.push(palabra);
        } else if (signosDeAgrupacion.includes(palabra)) {
            TsignosAgrupacion.push(palabra);
        } else if (asignacion.includes(palabra)) {
            Tasignacion.push(palabra);
        } else if (palabra.trim() === "") {
            continue; // Ignorar espacios en blanco
        } else {
            errores.push(palabra);
        }
    }

    console.log("Letras:", Tletras);
    console.log("Dígitos:", Tdigitos);
    console.log("Signos de Puntuación:", TsignosPuntuacion);
    console.log("Operadores Aritméticos:", ToperadoresAritmeticos);
    console.log("Operadores Racionales:", ToperadoresRacionales);
    console.log("Operadores Lógicos:", ToperadoresLogicos);
    console.log("Signos de Agrupación:", TsignosAgrupacion);
    console.log("Signos de Asignación:", Tasignacion);
    console.log("Errores:", errores);



//SEGUNDO CAMBIO 
var signosPuntuacion = [".", ",", ";", ":"];
var operadoresAritmeticos = ["^", "*", "/", "+", "-"];
var operadoresRacionales = ["<", ">", "<=", ">="];
var operadoresLogicos = ["AND", "&&", "OR", "||"];
var signosDeAgrupacion = ["(", ")", "[", "]", "{", "}"];

else if(operadoresAritmeticos.includes(parte)){
            lexemas.push(new Token(parte, "Operador Aritmetico"));
        } else if(operadoresRacionales.includes(parte)){
            lexemas.push(new Token(parte, "Operador Racional"))
        } else if(operadoresLogicos.includes(parte)){
            lexemas.push(new Token(parte, "Operador Logico"));
        } else if(signosDeAgrupacion.includes(parte)){
            lexemas.push(new Token(parte, "Signo de Agrupacion"));
        } else if(asignacion.includes(parte)){
            lexemas.push(new Token(parte, "Asignacion"))
        } else if(parte.trim() === " "){
            continue; // INGNORA LOS ESPACION
        } else {
            errores.push(parte);
        }


    for (let index = 0; index < errores.length; index++) {
        console.log(errores[index]);
    }

